/* DEFAULT VALUES */
let attributesDefault =
`
width
valign
align
border
cellspacing
cellpadding
nowrap
`

let tagsDefault =
`
p
`

let inputDefault =
`
<div class="content-area">
    <h2>Note to Readers</h2>
    <p>The financial results in this report are based on the audited consolidated financial statements of the <a href="https://www.canada.ca/en">Government of Canada</a> for the fiscal year ended March 31, 2023, the condensed form of which is included in this report. </p>
    <p>For the 25th consecutive year, the government has received an unmodified audit opinion from the <strong>Auditor General of Canada</strong> on the consolidated financial statements. The complete consolidated financial statements are available on the Public Services and Procurement Canada website.</p>
    <p>The <em>Fiscal Reference Tables</em> have been updated to incorporate the results for 2022–23 as well as historical revisions to the National Economic and Financial Accounts published by Statistics Canada.</p>
    <h2>Report Highlights</h2>
    <ul>
        <li>The government posted an annual operating deficit of $35.3 billion for the fiscal year ended March 31, 2023, compared to a deficit of $90.3&nbsp;billion in the previous fiscal year. The year-over-year improvement in the budgetary balance reflects the strong recovery of the Canadian economy from the effects of the pandemic along with the wind- down of temporary COVID-19 support measures. </li>
        <li>The annual operating deficit before net actuarial losses stood at $25.7 billion in 2022–23, compared to $80.1&nbsp;billion in 2021–22. The annual operating deficit before net actuarial losses is intended to supplement the traditional budgetary balance and improve the transparency of the government's financial reporting by isolating the impact of the recognition of net actuarial losses arising from the government's public sector pensions and other employee and veteran future benefits.</li>
        <li>Compared to projections in Budget 2023, the annual operating deficit was $7.7 billion lower than the <br>$43.0-billion deficit projected, mainly reflecting higher-than-expected tax revenues, partially offset by higher-than-expected program expenses and public debt charges. The annual operating deficit before net actuarial losses was $7.5 billion lower than projected.</li>
        <li>Compared to the 2021–22 fiscal year:<ul>
                <li>Revenues increased by $34.5 billion, or 8.4 per cent, reflecting a broad-based increase in revenue, supported by strong economic growth, tight labour markets, and resilient consumer spending. </li>
                <li>Program expenses excluding net actuarial losses decreased by $30.4 billion, or 6.5 per cent, largely reflecting lower transfers to individuals and businesses due to expiring temporary COVID-19 measures. </li>
                <li>Net actuarial losses, which reflect changes in the value of the government's obligations and assets for public sector pensions and other employee and veteran future benefits recorded in previous fiscal years, decreased $0.6 billion, or 5.5 per cent, largely reflecting the end of the amortization period for certain prior years' net actuarial losses in 2021–22. </li>
                <li>Public debt charges were up $10.5&nbsp;billion, or 42.7 per cent, largely reflecting higher interest rates on the stock of interest-bearing debt.</li>
            </ul>
        </li>
        <li>In 2022–23, the government recorded expenses totalling approximately $26 billion related to Indigenous claims, in advancing its commitment to resolve past injustices and renew its relationship with Indigenous Peoples. Absent these expenses, the 2022–23 budgetary deficit would have been roughly $9 billion, or 0.3 per cent of gross domestic product (GDP).</li>
        <li>The federal debt (the difference between total liabilities and total assets) stood at $1,173.0 billion at March&nbsp;31, 2023. The federal debt-to-GDP ratio was 42.2 per cent, down from 45.4 per cent in the previous year. As noted in Budget 2023, the government remains committed to its fiscal anchor of reducing the federal debt as a share of the economy over the medium term.</li>
        <li>For the 25th consecutive year, the government has received an unmodified audit opinion from the Auditor&nbsp;General of Canada on the consolidated financial statements.</li>
    </ul>
    <h3>Economic Context<sup><a href="#footnote-1" id="footnote-ref-1">[1]</a></sup></h3>
    <p>The Canadian economy continued its rapid recovery from the pandemic and was more resilient in 2022 than had been expected in the wake of rapid increases in interest rates. Canada saw the strongest growth in the G7 over the course of 2022. Canadian real GDP grew by 3.4 per cent in 2022 and was 3.7 per cent above its pre-pandemic level in the first quarter of 2023—the fastest recovery of the last four recessions. </p>
    <p>Solid economic growth helped an additional 750,000 Canadians find jobs between 2021 and 2022, pulling the unemployment rate to a 50-year low. More people aged 15 to 64 years are engaged in the labour market than ever before, with meaningful increases for historically under-represented groups, including women, newcomers, and young Canadians. The labour force participation rate among women aged 25 to 54 years reached record highs.</p>
    <p>Supply chain disruptions and surging commodity prices following Russia's invasion of Ukraine exacerbated economy-wide price pressures, with consumer price inflation peaking at 8.1&nbsp;per cent in June 2022. Since then, Canada has managed to make significant progress in reducing inflation, reaching 3.2 per cent in July 2023.</p>
    <p>Elevated consumer price inflation combined with high commodity prices and a resilient economy led to another year of double-digit growth in nominal GDP, the broadest measure of the tax base, at 10.9 per cent in 2022 after growing 13.6 per cent in 2021.</p>
    <p>Faced with inflationary pressures and excess demand in the economy, the Bank of Canada raised its policy rate by 425 basis points to 4.5 per cent between March 2022 and the end of 2022–23—the highest rate since 2007 and above its estimated "neutral range" of 2–3 per cent. </p>
    <p>Looking ahead, the lagged impacts of monetary policy tightening are expected to gradually build and broaden, slowing overall economic activity. Over time, slower growth is expected to help bring demand into balance with supply, gradually easing labour market tightness and slowing underlying inflation. Economic growth is expected to pick up over the course of 2024 as the effect of higher interest rates in Canada and globally dissipates. </p>
    <p>The government regularly surveys private sector economists on their views on the economy to assess and manage risk. The survey of private sector economists has been used as the basis for economic and fiscal planning since 1994 and introduces an element of independence into the government's forecasts. </p>
    <table>
        <tbody>
            <tr>
                <td></td>
                <td>
                    <p>2023 Budget<br>(Note 4)</p>
                </td>
                <td>
                    <p>2023 Actual</p>
                </td>
                <td>
                    <p>2022 Actual</p>
                    <p>Restated</p>
                    <p><strong>(Note 3)</strong></p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Revenues</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Income tax revenues</p>
                </td>
                <td>
                    <p>276,619</p>
                </td>
                <td>
                    <p>315,004</p>
                </td>
                <td>
                    <p>287,989</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Other taxes and duties</p>
                </td>
                <td>
                    <p>65,496</p>
                </td>
                <td>
                    <p>64,224</p>
                </td>
                <td>
                    <p>62,680</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Employment insurance premiums</p>
                </td>
                <td>
                    <p>25,801</p>
                </td>
                <td>
                    <p>26,914</p>
                </td>
                <td>
                    <p>23,856</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Proceeds from the pollution pricing framework</p>
                </td>
                <td>
                    <p>8,221</p>
                </td>
                <td>
                    <p>8,041</p>
                </td>
                <td>
                    <p>6,341</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Other revenues</p>
                </td>
                <td>
                    <p>32,254</p>
                </td>
                <td>
                    <p>33,632</p>
                </td>
                <td>
                    <p>32,411</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p><em>&nbsp;&nbsp;Total revenues</em></p>
                </td>
                <td>
                    <p><em>408,391</em></p>
                </td>
                <td>
                    <p><em>447,815</em></p>
                </td>
                <td>
                    <p><em>413,277</em></p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Expenses</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Transfer payments</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Old age security benefits and related payments</p>
                </td>
                <td>
                    <p>68,238</p>
                </td>
                <td>
                    <p>69,392</p>
                </td>
                <td>
                    <p>60,774</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Major transfer payments to other levels of government</p>
                </td>
                <td>
                    <p>89,827</p>
                </td>
                <td>
                    <p>90,784</p>
                </td>
                <td>
                    <p>88,386</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Employment insurance and support measures</p>
                </td>
                <td>
                    <p>31,270</p>
                </td>
                <td>
                    <p>21,836</p>
                </td>
                <td>
                    <p>38,923</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Children's benefits</p>
                </td>
                <td>
                    <p>25,166</p>
                </td>
                <td>
                    <p>24,553</p>
                </td>
                <td>
                    <p>26,226</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;COVID-19 income support for workers</p>
                </td>
                <td>
                    <p>410</p>
                </td>
                <td>
                    <p>(3,544)</p>
                </td>
                <td>
                    <p>15,582</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Canada emergency wage subsidy</p>
                </td>
                <td>
                    <p>–</p>
                </td>
                <td>
                    <p>(257)</p>
                </td>
                <td>
                    <p>22,291</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Proceeds from the pollution pricing framework returned</p>
                </td>
                <td>
                    <p>8,046</p>
                </td>
                <td>
                    <p>6,996</p>
                </td>
                <td>
                    <p>3,814</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Other transfer payments</p>
                </td>
                <td>
                    <p>86,263</p>
                </td>
                <td>
                    <p>99,199</p>
                </td>
                <td>
                    <p>88,478</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;<em>Total transfer payments</em></p>
                </td>
                <td>
                    <p><em>309,220</em></p>
                </td>
                <td>
                    <p><em>308,959</em></p>
                </td>
                <td>
                    <p><em>344,474</em></p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Other expenses, excluding net actuarial losses </p>
                </td>
                <td>
                    <p>116,173</p>
                </td>
                <td>
                    <p>129,596</p>
                </td>
                <td>
                    <p>124,445</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;<em>Total program expenses, excluding net actuarial losses</em></p>
                </td>
                <td>
                    <p><strong><em>425,393</em></strong></p>
                </td>
                <td>
                    <p><strong><em>438,555</em></strong></p>
                </td>
                <td>
                    <p><strong><em>468,919</em></strong></p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Public debt charges</p>
                </td>
                <td>
                    <p>26,904</p>
                </td>
                <td>
                    <p>34,955</p>
                </td>
                <td>
                    <p>24,487</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;<em>Total expenses, excluding net actuarial losses</em></p>
                </td>
                <td>
                    <p><strong><em>452,297</em></strong></p>
                </td>
                <td>
                    <p><strong><em>473,510</em></strong></p>
                </td>
                <td>
                    <p><strong><em>493,406</em></strong></p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Annual operating deficit before net actuarial losses </p>
                </td>
                <td>
                    <p>(43,906)</p>
                </td>
                <td>
                    <p>(25,695)</p>
                </td>
                <td>
                    <p>(80,129)</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;&nbsp;&nbsp;Net actuarial losses </p>
                </td>
                <td>
                    <p>(8,923)</p>
                </td>
                <td>
                    <p>(9,627)</p>
                </td>
                <td>
                    <p>(10,186)</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Annual operating deficit </p>
                </td>
                <td>
                    <p>(52,829)</p>
                </td>
                <td>
                    <p>(35,322)</p>
                </td>
                <td>
                    <p>(90,315)</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Accumulated operating deficit at beginning of year</p>
                </td>
                <td>
                    <p>(1,148,296)</p>
                </td>
                <td>
                    <p>(1,148,296)</p>
                </td>
                <td>
                    <p>(1,054,125)</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Other comprehensive income </p>
                </td>
                <td>
                    <p>–</p>
                </td>
                <td>
                    <p>–</p>
                </td>
                <td>
                    <p><strong>4,465</strong></p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Accumulated operating deficit at end of year</p>
                </td>
                <td>
                    <p>(1,201,125)</p>
                </td>
                <td>
                    <p>(1,183,618)</p>
                </td>
                <td>
                    <p><strong>(1,139,975)</strong></p>
                </td>
            </tr>
        </tbody>
    </table>
    <p>Table 1</p>
    <p>Average Private Sector Forecasts</p>
    <p>per cent</p>
    <table>
        <tbody>
            <tr>
                <td></td>
                <td>
                    <p>2021</p>
                </td>
                <td>
                    <p>2022</p>
                </td>
                <td>
                    <p>2023</p>
                </td>
                <td>
                    <p>2024</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Real GDP growth</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2022</p>
                </td>
                <td>
                    <p>5.0</p>
                </td>
                <td>
                    <p>4.1</p>
                </td>
                <td>
                    <p>3.1</p>
                </td>
                <td>
                    <p>2.0</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2023</p>
                </td>
                <td>
                    <p>5.0</p>
                </td>
                <td>
                    <p>3.4</p>
                </td>
                <td>
                    <p>0.3</p>
                </td>
                <td>
                    <p>1.5</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Actual</p>
                </td>
                <td>
                    <p>5.0</p>
                </td>
                <td>
                    <p>3.4</p>
                </td>
                <td>
                    <p>– </p>
                </td>
                <td>
                    <p>– </p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Nominal GDP growth</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2022</p>
                </td>
                <td>
                    <p>13.6</p>
                </td>
                <td>
                    <p>8.1</p>
                </td>
                <td>
                    <p>4.8</p>
                </td>
                <td>
                    <p>3.9</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2023</p>
                </td>
                <td>
                    <p>13.6</p>
                </td>
                <td>
                    <p>10.9</p>
                </td>
                <td>
                    <p>0.8</p>
                </td>
                <td>
                    <p>3.6</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Actual</p>
                </td>
                <td>
                    <p>13.6</p>
                </td>
                <td>
                    <p>10.9</p>
                </td>
                <td>
                    <p>– </p>
                </td>
                <td>
                    <p>– </p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>3-month treasury bill rate</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2022</p>
                </td>
                <td>
                    <p>0.1</p>
                </td>
                <td>
                    <p>0.8</p>
                </td>
                <td>
                    <p>1.7</p>
                </td>
                <td>
                    <p>2.0</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2023</p>
                </td>
                <td>
                    <p>0.1</p>
                </td>
                <td>
                    <p>2.4</p>
                </td>
                <td>
                    <p>4.4</p>
                </td>
                <td>
                    <p>3.3</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Actual</p>
                </td>
                <td>
                    <p>0.1</p>
                </td>
                <td>
                    <p>2.4</p>
                </td>
                <td>
                    <p>– </p>
                </td>
                <td>
                    <p>– </p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>10-year government bond rate</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2022</p>
                </td>
                <td>
                    <p>1.4</p>
                </td>
                <td>
                    <p>2.0</p>
                </td>
                <td>
                    <p>2.4</p>
                </td>
                <td>
                    <p>2.6</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2023</p>
                </td>
                <td>
                    <p>1.4</p>
                </td>
                <td>
                    <p>2.8</p>
                </td>
                <td>
                    <p>3.0</p>
                </td>
                <td>
                    <p>2.9</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Actual</p>
                </td>
                <td>
                    <p>1.4</p>
                </td>
                <td>
                    <p>2.8</p>
                </td>
                <td>
                    <p>– </p>
                </td>
                <td>
                    <p>– </p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Unemployment rate</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2022</p>
                </td>
                <td>
                    <p>7.5</p>
                </td>
                <td>
                    <p>5.8</p>
                </td>
                <td>
                    <p>5.5</p>
                </td>
                <td>
                    <p>5.5</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2023</p>
                </td>
                <td>
                    <p>7.5</p>
                </td>
                <td>
                    <p>5.3</p>
                </td>
                <td>
                    <p>5.8</p>
                </td>
                <td>
                    <p>6.2</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Actual</p>
                </td>
                <td>
                    <p>7.5</p>
                </td>
                <td>
                    <p>5.3</p>
                </td>
                <td>
                    <p>– </p>
                </td>
                <td>
                    <p>– </p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>Consumer Price Index inflation</p>
                </td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2022</p>
                </td>
                <td>
                    <p>3.4</p>
                </td>
                <td>
                    <p>3.9</p>
                </td>
                <td>
                    <p>2.4</p>
                </td>
                <td>
                    <p>2.2</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Budget 2023</p>
                </td>
                <td>
                    <p>3.4</p>
                </td>
                <td>
                    <p>6.8</p>
                </td>
                <td>
                    <p>3.5</p>
                </td>
                <td>
                    <p>2.1</p>
                </td>
            </tr>
            <tr>
                <td>
                    <p>&nbsp;&nbsp;Actual</p>
                </td>
                <td>
                    <p>3.4</p>
                </td>
                <td>
                    <p>6.8</p>
                </td>
                <td>
                    <p>– </p>
                </td>
                <td>
                    <p>– </p>
                </td>
            </tr>
        </tbody>
    </table>
    <p>Note: Figures have been restated to reflect the historical revisions in the Canadian System of National Accounts as of the first quarter of 2023 released on May&nbsp;31, 2023.</p>
    <table>
    <tbody>
        <tr>
            <td>
                <p><strong>Impacted Canadians</strong></p>
            </td>
            <td>
                <p><strong>Actions</strong></p>
            </td>
        </tr>
        <tr>
            <td>
                <p>Workers who lose jobs</p>
            </td>
            <td>
                <ul>
                    <li><a href="https://www.canada.ca/en/employment-social-development/programs/ei.html"><strong>Ensure a strong social safety net</strong></a><strong> </strong>through the Employment Insurance (EI) program with built-in stabilizers to reflect regional unemployment rate.</li>
                    <li><a href="https://www.canada.ca/en/employment-social-development/programs/ei/work-sharing.html"><strong>Introduce temporary flexibilities</strong></a><strong> under the EI Work-Sharing Program</strong>, which provides EI benefits to employees who work reduced hours due to a decrease in business activity beyond their employer's control. This helps employers and employees avoid layoffs in the first place. </li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>
                <p>Businesses</p>
            </td>
            <td>
                <ul>
                    <li><a href="https://canada-preview.adobecqms.net/en/department-finance/news/2025/03/fighting-for-canadian-workers-and-businesses.html"><strong>Expand access to existing liquidity supports</strong></a><strong> </strong>for businesses through financial Crown corporations.</li>
                    <li><a href="https://www.edc.ca/"><strong>Export Development Canada (EDC</strong></a><strong>) </strong>– $5 billion for new Trade Impact Program, for Canadian exporters and companies that supply to exporters.</li>
                    <li><a href="https://www.bdc.ca/en"><strong>Business Development Bank of Canada (BDC</strong></a><strong>)</strong> – $500 million for favourably priced loans, for financial and non-financial support to Canadian businesses.</li>
                    <li><a href="https://www.fcc-fac.ca/en"><strong>Farm Credit Canada (FCC)</strong></a><strong> </strong>– $1 billion in new financing, for a range of financing products and services to Canadian farmers. </li>
                    <li><a href="https://www.canada.ca/en/department-finance/programs/international-trade-finance-policy/process-requesting-remission-tariffs-that-apply-on-certain-goods-us.html"><strong>Introduce a remission framework</strong></a> to protect businesses and jobs.</li>
                    <li><strong>Update the </strong><a href="https://ised-isde.canada.ca/site/investment-canada-act/en/investment-canada-act/guidelines/guidelines-national-security-review-investments"><strong><em>Investment Canada Act</em></strong></a><strong> Guidelines.</strong></li>
                    <li><strong>Use revenues</strong> from Canada's countermeasures to directly support Canadians and Canadian businesses.</li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>
                <p>Hardest hit sectors</p>
            </td>
            <td>
                <ul>
                    <li><strong>Consider expanding and broadening existing supports</strong> to maximize reach as the economic impacts unfold. </li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>
                <p>Hardest hit regions</p>
            </td>
            <td>
                <ul>
                    <li><strong>Explore leveraging </strong><a href="https://ised-isde.canada.ca/site/ised/en/canadas-regional-development-agencies"><strong>regional development agencies</strong></a><strong> </strong>to provide supports that address unique regional impacts.</li>
                    <li><strong>Work with provinces and territories</strong> to ensure supports are in place across all jurisdictions.</li>
                </ul>
            </td>
        </tr>
        <tr>
            <td>
                <p>Consumers</p>
            </td>
            <td>
                <ul>
                    <li><strong>Empower consumers to support Canadian businesses </strong>by increasing awareness of products made in Canada.</li>
                </ul>
            </td>
        </tr>
    </tbody>
</table>
    <ol>
        <li id="footnote-1">
            <p> This section incorporates data available up to and including August 15, 2023. The annual results are on a calendar year basis. <a href="#footnote-ref-1">↑</a></p>
        </li>
    </ol>
</div>
`

const inputHTML = document.createElement('div');
var xStartPos, xEndPos;
var yStartPos, yEndPos;
var toggledCells = 0;
var isMouseDown = false;

var currentVisualTable;

/**
 * Performs script after the window has loaded
 */
window.onload = function () {
    /**
     * Create event listeners for script trigger and user interaction
     */
     function createListeners() {
        const cleanupBtn = document.getElementById('cleanupBtn');
        const results = document.getElementById('results');
        const outputText = document.getElementById('outputText');
        const loadHTMLBtn = document.getElementById('loadHTMLBtn');
        const copyBtn = document.getElementById('copyBtn');
        const copyBtn2 = document.getElementById('copyBtn2');
        const visual = document.getElementById('visual');
        const tableNum = document.getElementById('tableNum');
        const tableTitle = document.getElementById('tableTitle');
        const tableUnit = document.getElementById('tableUnit');

        const finTable = document.getElementById('finTable')

        const deselectBtn = document.getElementById('deselectBtn');
        const theadBtn = document.getElementById('theadBtn')
        const mergeRowBtn = document.getElementById('mergeRowBtn');
        const activeBtn = document.getElementById('activeBtn');
        const mergeBtn = document.getElementById('mergeBtn');
        const tfootBtn = document.getElementById('tfootBtn');
        const indentBtn = document.getElementById('indentBtn');
        const outdentBtn = document.getElementById('outdentBtn');
        const boldBtn = document.getElementById('boldBtn');
        const leftAlignBtn = document.getElementById('leftAlignBtn');
        const centerAlignBtn = document.getElementById('centerAlignBtn');
        const rightAlignBtn = document.getElementById('rightAlignBtn');
        const deleteRowBtn = document.getElementById('deleteRowBtn');

        const tableToolbar = document.getElementById('tableToolbar');

        loadHTMLBtn.addEventListener('click', () => {
            inputText.value = getInputFromSession();
        });

        tableNum.addEventListener('input', () => {
            const tableDiv = visual.querySelector('div.table-responsive');
            if (!tableDiv) return;

            const caption = tableDiv.querySelector('caption');
            if (caption) {
                // update existing caption
                const captionNodes = caption.childNodes;

                if (captionNodes[0] && captionNodes[0].nodeType === Node.TEXT_NODE) { 
                    captionNodes[0].nodeValue = `${tableNum.value}`;
                    if (captionNodes[0].nodeValue.trim() == "") {
                        if (captionNodes[0].nextSibling && captionNodes[0].nextSibling.nodeName === "BR") {
                            caption.removeChild(captionNodes[0].nextSibling);
                        }
                        caption.removeChild(captionNodes[0]);
                    }
                } else {
                    const newTableNum = document.createTextNode(`${tableNum.value}`);
                    caption.insertBefore(document.createElement('br'), captionNodes[0]);
                    caption.insertBefore(newTableNum, captionNodes[0]);
                }
            } else if (tableNum.value.trim()) {
                // create caption
                const table = tableDiv.querySelector('table');
                const thead = table.querySelector('thead');
                const tbody = table.querySelector('tbody');
                const newCaption = document.createElement('caption');

                newCaption.classList.add('text-left', 'fnt-nrml');
                newCaption.innerHTML = tableNum.value;
                
                table.insertBefore(newCaption, thead ? thead : tbody);
            }

            if (caption && caption.innerText.trim() === "") {
                caption.parentNode.removeChild(caption);
            }
            
            copyVisualHTML(tableDiv);
            displayOutput(inputHTML);
        });

        tableTitle.addEventListener('input', () => {
            const tableDiv = visual.querySelector('div.table-responsive');
            if (!tableDiv) return;

            const caption = tableDiv.querySelector('caption');
            if (caption) {
                // update existing caption
                const strong = caption.querySelector('strong');

                if (strong) {
                    strong.textContent = `${tableTitle.value}`;
                    if (!strong.innerHTML) {
                        if (strong.previousSibling && strong.previousSibling.nodeName === "BR") {
                            caption.removeChild(strong.previousSibling);
                        } else if (strong.nextSibling && strong.nextSibling.nodeName === "BR") {
                            caption.removeChild(strong.nextSibling);
                        }
                        caption.removeChild(strong);
                    }
                } else {
                    const newStrong = document.createElement('strong');
                    newStrong.textContent = `${tableTitle.value}`;
                    const small = caption.querySelector('small');
                    if (small) {
                        caption.insertBefore(newStrong, small);
                        caption.insertBefore(document.createElement('br'), small);
                    } else {
                        caption.appendChild(newStrong);
                        if (caption.childNodes[0] && caption.childNodes[0].nodeType === Node.TEXT_NODE) {
                            caption.insertBefore(document.createElement('br'), newStrong);
                        }
                    }
                }
            } else if (tableTitle.value.trim()) {
                // create caption
                const table = tableDiv.querySelector('table');
                const thead = table.querySelector('thead');
                const tbody = table.querySelector('tbody');
                const newCaption = document.createElement('caption');

                newCaption.classList.add('text-left', 'fnt-nrml');
                newCaption.innerHTML = `<strong>${tableTitle.value}</strong>`;
                
                table.insertBefore(newCaption, thead ? thead : tbody);
            }

            if (caption && caption.innerText.trim() === "") {
                caption.parentNode.removeChild(caption);
            }

            copyVisualHTML(tableDiv);
            displayOutput(inputHTML);
        });
        
        tableUnit.addEventListener('input', () => {
            const tableDiv = visual.querySelector('div.table-responsive');
            if (!tableDiv) return;
            
            const caption = tableDiv.querySelector('caption');
            if (caption) {
                // update existing caption
                const small = caption.querySelector('small');
                
                if (small) {
                    small.textContent = `${tableUnit.value}`;
                    if (!small.innerHTML) {
                        if (small.previousSibling && small.previousSibling.nodeName === "BR") {
                            caption.removeChild(small.previousSibling);
                        }
                        caption.removeChild(small);
                    }
                } else {
                    const newSmall = document.createElement('small');
                    newSmall.textContent = `${tableUnit.value}`;
                    caption.appendChild(newSmall);
                    if (caption.childNodes[0] && (caption.childNodes[0].nodeType === Node.TEXT_NODE || caption.childNodes[0].nodeName === "STRONG")) {
                        caption.insertBefore(document.createElement('br'), newSmall);
                    }
                }
            } else if (tableUnit.value.trim()) {
                // create caption
                const table = tableDiv.querySelector('table');
                const thead = table.querySelector('thead');
                const tbody = table.querySelector('tbody');
                const newCaption = document.createElement('caption');

                newCaption.classList.add('text-left', 'fnt-nrml');
                newCaption.innerHTML = `<small>${tableUnit.value}</small>`;
                
                table.insertBefore(newCaption, thead ? thead : tbody);
            }

            if (caption && caption.innerText.trim() === "") {
                caption.parentNode.removeChild(caption);
            }

            copyVisualHTML(tableDiv);
            displayOutput(inputHTML);
        });

        finTable.addEventListener('click', () => {
            const tagText = document.getElementById('tagList');
            let tags = tagText.value.split('\n');

            if (finTable.checked) {
                if (!tags.includes('p')) {
                    tagText.value += 'p';
                }
            } else {
                if (tags.includes('p')) {
                    tags.splice(tags.indexOf('p'), 1);
                    for (var tag of tags) {
                        tagText.value = `${tag}\n`;
                    }
                }
            }
        });

        cleanupBtn.addEventListener('click', () => {
            const moveToTfootChecked = document.getElementById('moveToTfoot').checked; 
            parseInput();
            cleanInput();

            if (moveToTfootChecked) {
                let tbody = visual.querySelector('tbody');
                let row = tbody.querySelector('tr:last-child');

                let cells = row.querySelector('th, td');
                if (cells) {
                    cells.classList.add('selected');
                }

                moveToTfoot();
            }

            // Add selection listeners to all table cells
            let cells = visual.querySelectorAll('th, td:not(tfoot td)');
            cells.forEach(e => {
                addVisualCellListener(e);
            });

            // Show results and scroll to it
            results.classList.remove('wb-inv');
            scrollSmoothTo(visual);
        });

        // link 'click' event listers to function
        deselectBtn.addEventListener('click', deselectCells);
        theadBtn.addEventListener('click', toggleThead);
        mergeRowBtn.addEventListener('click', mergeAllCellsInRow);
        activeBtn.addEventListener('click', toggleActiveClass);
        mergeBtn.addEventListener('click', mergeCellsInRow);
        tfootBtn.addEventListener('click', moveToTfoot);
        indentBtn.addEventListener('click', increaseIndent);
        outdentBtn.addEventListener('click', decreaseIndent);
        boldBtn.addEventListener('click', handleBold);
        leftAlignBtn.addEventListener('click', () => handleAlign('left'));
        centerAlignBtn.addEventListener('click', () => handleAlign('center'));
        rightAlignBtn.addEventListener('click', () => handleAlign('right'));
        deleteRowBtn.addEventListener('click', deleteRow);
        copyBtn.addEventListener('click', () => {
            copyToClipboard(outputText);
        });
        copyBtn2.addEventListener('click', () => {
            copyToClipboard(outputText);
        });

        // deselect all if click outside of visual
        document.addEventListener('click', (event) => {
            if (!visual.contains(event.target) && !tableToolbar.contains(event.target)) {
                deselectCells();
            }
        });
    }

    /**
     * Removes selected class from all elements
     */
     function deselectCells() {
        const visual = document.getElementById('visual');
        const tableToolbar = document.getElementById('tableToolbar');

        let cells = visual.querySelectorAll('th, td');
        for (var cell of cells) {
            cell.classList.remove('hovered');
        }

        let selected = visual.querySelectorAll('.selected');
        for (var cell of selected) {
            cell.classList.remove('selected');
            if (cell.classList.length <= 0) {
                cell.removeAttribute('class');
            }
        }
        
        
        tableToolbar.classList.add('wb-inv');
    }

    function addVisualCellListener(e) {
        // e.addEventListener('click', () => {
        //     e.classList.toggle('selected');
        //     tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);
        // })
        var xCellPos, yCellPos;


        const allRows = visual.querySelectorAll('tr');

        for (var y = 0; y < allRows.length; y++) {
            const allCellsInRow = allRows[y].querySelectorAll('th, td');
            for (var x = 0; x < allCellsInRow.length; x++) {
                if (allCellsInRow[x] == e) {
                    xCellPos = x;
                    yCellPos = y;
                }
            }
        }

        e.addEventListener('mousedown', () => {
            toggledCells = 0;
            for (var y = 0; y < allRows.length; y++) {
                const allCellsInRow = allRows[y].querySelectorAll('th, td');
                for (var x = 0; x < allCellsInRow.length; x++) {
                    if (allCellsInRow[x] == e) {
                        xStartPos = x;
                        yStartPos = y;
                        isMouseDown = true;
                        // e.classList.toggle('selected');
                        // tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);
                        break;
                    }
                }
            }
        });

        e.addEventListener('mouseenter', () => {
            if (!isMouseDown) return;
            
            for (var y = 0; y < allRows.length; y++) {
                const allCellsInRow = allRows[y].querySelectorAll('th, td');
                for (var x = 0; x < allCellsInRow.length; x++) {
                    if (allCellsInRow[x] == e) {
                        xEndPos = x;
                        yEndPos = y;
                        break;
                    }
                }
            }

            xMin = Math.min(xStartPos, xEndPos);
            xMax = Math.max(xStartPos, xEndPos);
            yMin = Math.min(yStartPos, yEndPos);
            yMax = Math.max(yStartPos, yEndPos);

            for (var y = 0; y < allRows.length; y++) {
                const allCellsInRow = allRows[y].querySelectorAll('th, td');
                skipColSpan = 0;
                for (var x = 0; x < allCellsInRow.length; x++) {
                    if (x >= xMin && x <= xMax - skipColSpan && y >= yMin && y <= yMax) {
                        if (!allCellsInRow[x]) break;

                        if (!allCellsInRow[x].classList.contains('selected')) {
                            allCellsInRow[x].classList.add('hovered');
                        }

                        if (allCellsInRow[x].hasAttribute('colspan')) {
                            skipColSpan += allCellsInRow[x].getAttribute('colspan') - 1 - x;
                        }
                    } else {
                        allCellsInRow[x].classList.remove('hovered');
                    }
                    
                }
            }
        });

        e.addEventListener('mouseup', () => {
            var xMin, xMax;
            var yMin, yMax;

            var skipColSpan = 0;

            for (var y = 0; y < allRows.length; y++) {
                const allCellsInRow = allRows[y].querySelectorAll('th, td');
                for (var x = 0; x < allCellsInRow.length; x++) {
                    if (allCellsInRow[x] == e) {
                        xEndPos = x;
                        yEndPos = y;
                        isMouseDown = false;
                        break;
                    }
                }
            }

            if (xStartPos - xEndPos == 0 && yStartPos - yEndPos == 0 && e.classList.contains('selected')) {
                e.classList.remove('selected');
                tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);
                return;
            }

            xMin = Math.min(xStartPos, xEndPos);
            xMax = Math.max(xStartPos, xEndPos);
            yMin = Math.min(yStartPos, yEndPos);
            yMax = Math.max(yStartPos, yEndPos);
            
            for (var y = yMin; y <= yMax; y++) {
                const allCellsInRow = allRows[y].querySelectorAll('th, td');
                skipColSpan = 0;
                for (var x = xMin; x <= xMax - skipColSpan; x++) {
                    if (!allCellsInRow[x]) break;
                    toggledCells++;
                    allCellsInRow[x].classList.add('selected');
                    allCellsInRow[x].classList.remove('hovered');
                    tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);
                    if (allCellsInRow[x].hasAttribute('colspan')) {
                        skipColSpan += allCellsInRow[x].getAttribute('colspan') - 1 - x;
                    }
                }
            }

        });        
    }

    /**
     * Checks selected rows and moves to thead or tbody
     */
    function toggleThead() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');
        const thead = visual.querySelector('thead');
        const tbody = visual.querySelector('tbody');

        let tbodyTRs = tbody.querySelectorAll('tr');
        let theadTRs = thead.querySelectorAll('tr');

        moveTRToThead(tbodyTRs, thead);
        moveTRToTbody(theadTRs, tbody);

        deselectCells();
        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    

    /**
     * Moves input table rows to the bottom of the table head
     * @param {*} moveTRs NodeList of TR DOM elements
     * @param {*} thead THEAD DOM element receiving the table rows
     */
    function moveTRToThead(moveTRs, thead) {
        const finTableChecked = document.getElementById('finTable').checked;
        const scopeChecked = document.getElementById('scope').checked;

        for (var moveTR of moveTRs) {
            if (moveTR.querySelector('.selected')) {
                moveTR.classList.add('bg-dark', 'text-white');
                moveTR.classList.remove('active');
        
                var count = 0;

                moveTR.querySelectorAll('th, td').forEach((node) => {
                    if (finTableChecked && count > 0 && node.nodeName === 'TD') {
                        node.classList.add('text-right');
                    }
                    var newNode = renameTag(node, 'th');
                    // add 'text-right' class to <th> if financial table
                    if (scopeChecked) {
                        newNode.setAttribute('scope', 'col');
                    }

                    count++;
                });
                thead.appendChild(moveTR);
            }
        }
    }

     /**
     * Moves input table rows to the top of the table body
     * @param {*} moveTRs NodeList of TR DOM elements
     * @param {*} tbody TBODY DOM element receiving the table rows
     */
      function moveTRToTbody(moveTRs, tbody) {
        const finTableChecked = document.getElementById('finTable').checked;
        const scopeChecked = document.getElementById('scope').checked;

        for (var moveTR of moveTRs) {
            if (moveTR.querySelector('.selected')) {
                moveTR.classList.remove('bg-dark', 'text-white');
        
                let ths = moveTR.querySelectorAll('th');
                for (var i = 0; i < ths.length; i++) {
                    if (i === 0) {
                        if (scopeChecked) {
                            ths[i].setAttribute('scope', 'row');
                        }
                        continue;
                    }
                    var newNode = renameTag(ths[i], 'td');
                    // add 'text-right' class to <td> if financial table
                    if (finTableChecked) {
                        newNode.classList.remove('text-right');
                        var tds = moveTR.querySelectorAll('td');
                        for (var td of tds) {
                            td.classList.add('text-right');
                        }
                    }
                }
                tbody.insertBefore(moveTR, tbody.querySelector('tr'));
            }
        }
    }

    /**
     * Checks selected rows and moves to tfoot
     */
    function moveToTfoot() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');
        const tbody = visual.querySelector('tbody');
        
        let tbodyTRs = tbody.querySelectorAll('tr');
        
        moveTRToTfoot(tbodyTRs);
        
        const tableToolbar = document.getElementById('tableToolbar'); 
        tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);

        deselectCells();
        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Moves selected table rows to a 'p' tag in Tfoot. Creates a tfoot if one doesn't exist
     * @param {*} moveTRs table rows that are selected to be moved
     */
    function moveTRToTfoot(moveTRs) {
        const visual = document.getElementById('visual');
        let tfoot = visual.querySelector('tfoot');
        const table = visual.querySelector('table');

        if (!tfoot) {
            addTfoot(table, false);
            tfoot = table.querySelector('tfoot');
        }
        for (var moveTR of moveTRs) {
            let cell = moveTR.querySelector('.selected');
            if (cell) {
                let tfootTD = tfoot.querySelector('tr > td');
                let p = document.createElement('p');
                
                p.innerHTML = cell.innerHTML;
                tfootTD.appendChild(p);

                moveTR.parentNode.removeChild(moveTR)
            }
        }
    }

    /**
     * Checks selected rows and deletes the row
     */
     function deleteRow() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');
        const tbody = visual.querySelector('tbody');
        
        let tbodyTRs = tbody.querySelectorAll('tr');
        
        deleteTRs(tbodyTRs);
        
        const tableToolbar = document.getElementById('tableToolbar'); 
        tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);

        deselectCells();
        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Deletes selected table rows
     * @param {*} deleteTRs table rows that are selected to be deleted
     */
     function deleteTRs(deleteTRs) {
        for (var tr of deleteTRs) {
            let cell = tr.querySelector('.selected');
            if (cell) {
                tr.parentNode.removeChild(tr)
            }
        }
    }

    /**
     * Gets all selected elements and toggles bold style
     */
    function handleBold() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');

        let selected = visual.querySelectorAll('.selected');

        var allBold = 1;

        // jordan - "Word-like" bolding method
        for (var cell of selected) {
            if (cell.nodeName == 'TH') {
                if (cell.classList.contains('fnt-nrml')) {
                    allBold = 0;
                    for (var c of selected) {
                        setBold(c, true);
                    }
                    break;
                }
            }

            else if (cell.firstChild.nodeName != 'STRONG' || cell.classList.contains('fnt-nrml')) {
                allBold = 0;
                for (var c of selected) {
                    setBold(c, true);
                }
                break;
            }
        }

        if (allBold) {
            for (var cell of selected) {
                setBold(cell, false);
            }
        }

        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Sets or remove bold style to cell depending on the value
     * @param {*} cell TD or TH DOM element
     * @param {*} value true or false
     */
    function setBold(cell, value) {
        // value true
        if (value) {
            // th: remove fnt-nrml class
            if (cell.nodeName === 'TH' || cell.nodeName === 'DIV') {
                cell.classList.remove('fnt-nrml');
            }
            // td: check if strong tag doesn't exist
            else if (cell.firstChild && cell.firstChild.nodeName != 'STRONG') {
                let strong = document.createElement('strong');

                while (cell.firstChild) {
                    strong.appendChild(cell.firstChild);
                }

                cell.appendChild(strong);
            }

        }

        // value false, remove bold
        else {
            // th: add fnt-nrml class
            if (cell.nodeName === 'TH' || cell.nodeName === 'DIV') {
                cell.classList.add('fnt-nrml');
            }
            // td: remove strong tag
            else if (cell.firstChild && cell.firstChild.nodeName == 'STRONG') {
                removeTag(cell.firstChild, 'strong');
            }
            else if (cell.firstChild && cell.firstChild.nodeName == 'B') {
                removeTag(cell.firstChild, 'b');
            }
        }
    }

    /**
     * Checks current indent level of selected elements and increases indent accordingly
     */
    function increaseIndent() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');

        var mrgnLvl = ['mrgn-lft-md', 'mrgn-lft-lg', 'mrgn-lft-xl']

        // use jquery to look for selected element that does not have either of the 3 mrgn classes
        const noMargin    = $("tbody th.selected:not(:has(." + mrgnLvl[0] + ",." + mrgnLvl[1] + ",." + mrgnLvl[2] + "))");
        const subHeading  = visual.querySelectorAll('tbody th.selected div.' + mrgnLvl[0]);
        const sub2Heading = visual.querySelectorAll('tbody th.selected div.' + mrgnLvl[1]);


        // create margin div in these nodes
        for (var node of noMargin) {
            let indent = document.createElement('div');
                indent.classList.add('text-left', 'fnt-nrml', 'mrgn-lft-md');

                // move node's children to new div
                while (node.firstChild) {
                    indent.appendChild(node.firstChild);
                }

                node.appendChild(indent);
        }

        for (var node of subHeading) {
            setIndent(node, mrgnLvl[1], mrgnLvl[0]);
        }

        for (var node of sub2Heading) {
            setIndent(node, mrgnLvl[2], mrgnLvl[1]);
        }

        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Checks current indent level of selected elements and decreases indent accordingly
     */
    function decreaseIndent() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');

        var mrgnLvl = ['mrgn-lft-md', 'mrgn-lft-lg', 'mrgn-lft-xl']

        const subHeading  = visual.querySelectorAll('tbody th.selected div.' + mrgnLvl[0]);
        const sub2Heading = visual.querySelectorAll('tbody th.selected div.' + mrgnLvl[1]);
        const sub3Heading = visual.querySelectorAll('tbody th.selected div.' + mrgnLvl[2]);

        for (var node of subHeading) {
            node.classList.remove('mrgn-lft-md', 'text-left', 'fnt-nrml');
            if (node.classList.length <= 0) {
                removeTag(node, 'div');
            }
        }

        for (var node of sub2Heading) {
            setIndent(node, mrgnLvl[0], mrgnLvl[1]);
        }

        for (var node of sub3Heading) {
            setIndent(node, mrgnLvl[1], mrgnLvl[2]);
        }

        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Sets the node's indent level
     * @param {*} node node that is changing indent level
     * @param {*} newClass new indent level
     * @param {*} oldClass old indent level
     */
    function setIndent(node, newClass, oldClass) {
        if (node && oldClass) {
            node.classList.remove(oldClass);
        }
        if (node && newClass) {
            node.classList.add(newClass);
        }
    }

    function mergeAllCellsInRow() {
        const visual = document.getElementById('visual');

        const tbody = visual.querySelector('tbody');
        const rows = tbody.querySelectorAll('tr');

        for (var row of rows) {
            if (row.querySelector('.selected')) {
                const cells = row.querySelectorAll('th, td');

                for (var cell of cells) {
                    cell.classList.add('selected');
                }
                mergeCellsInRow();
            }
        }
    }

    /**
     * Checks if a cell in the row is selected and toggles the active class on the row
     */
    function toggleActiveClass() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');

        let tbody = visual.querySelector('tbody');

        let rows = tbody.querySelectorAll('tr');

        for (var row of rows) {
            if (row.querySelector('.selected')) {
                setAsActive(row);
            }
        }

        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Toggles 'active' class for <tr> element
     * @param {*} row TR DOM element
     */
    function setAsActive(row) {
        const scopeChecked = document.getElementById('scope').checked;

        row.classList.toggle('active');
        
        if (row.classList <= 0) {
            row.removeAttribute("class");
        }

        let cells = row.querySelectorAll('th, td');
        for (var cell of cells) {
            if (cell.nodeName === 'TH') {
                cell.classList.toggle('fnt-nrml', !row.classList.contains('active'));
                if (scopeChecked) {
                    if (row.classList.contains('active')) {
                        cell.setAttribute('scope', 'colgroup');
                    } else {
                        cell.setAttribute('scope', 'row');
                    }
                }
            }
            else {
                setBold(cell, row.classList.contains('active'));
            }
        }
    }

    /**
     * Checks each row if there is a selected cell, perform merge if more than one is selected
     */
    function mergeCellsInRow() {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');

        let tbody = visual.querySelector('tbody');

        let rows = tbody.querySelectorAll('tr');

        for (var row of rows) {
            let selectedInRow = row.querySelectorAll('.selected');
            if (selectedInRow.length > 1) {
                mergeCells(selectedInRow);
            }
        }

        deselectCells();
        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Merge cells into one cell
     * Adds colspan with the value of the num of other deleted 'selected' cells
     * @param {*} selected cells with selected class
     */
    function mergeCells(selected) {
        let firstCell = selected[0];
        let count = 1; // start at 1 to include firstCell

        if (firstCell.hasAttribute('colspan')) {
            count = +firstCell.getAttribute('colspan');
        }

        // remove all selected except for first
        for (var i = 1; i < selected.length; i++) {
            if (selected[i].hasAttribute('colspan')) {
                count += +selected[i].getAttribute('colspan');
            } else {
                count += 1;
            }
            firstCell.parentNode.removeChild(selected[i]);
        }

        // set colspan to count (num of deleted cells)
        firstCell.setAttribute('colspan', count);
    }

    /**
     * Removes all other text alignment classes and toggles the text-center class of selected cells
     */
    function handleAlign(alignment) {
        const visual = document.getElementById('visual');
        const tableDiv = visual.querySelector('div.table-responsive');

        let selected = visual.querySelectorAll('.selected');

        for (var cell of selected) {
            if (alignment == 'left') {
                cell.classList.remove('text-right');
                cell.classList.remove('text-center');
            } else if (alignment == 'center') {
                cell.classList.remove('text-right');
                cell.classList.add('text-center');
            } else if (alignment == 'right') {
                cell.classList.add('text-right');
                cell.classList.remove('text-center');
            }
        }

        var node = copyVisualHTML(tableDiv);
        displayOutput(inputHTML);
    }

    /**
     * Clones the node while removing .selected classes
     * @param {*} node node to be copied
     * @returns clone node without selected class in all elements
     */
    function copyVisualHTML(node) {
        let clone = node.cloneNode(true);

        clone.querySelectorAll('.selected').forEach((e) => {
            e.classList.remove('selected');
            if (e.classList.length <= 0) {
                e.removeAttribute('class');
            }
        });

        currentVisualTable.innerHTML = clone.innerHTML;

        return clone;
    }

    /**
     * Parses input text content into an HTML DOM
     * @returns html dom
     */
    function parseInput() {
        const htmlText = document.getElementById('inputText');

        inputHTML.innerHTML = htmlText.value.trim();
    }

    /**
     * Loop through all elements in the HTML DOM structure and clean it
     * @param {*} html HTML DOM Structure
     */
     function cleanInput() {
        const formatChecked = document.getElementById('format').checked;

        if (inputHTML) {
            let tables = inputHTML.querySelectorAll('table');
            
            tables.forEach((t) => {
                if (formatChecked) {
                    var cleanTable = formatTable(t);
                    console.log(cleanTable);
                    console.log(t.parentNode);
                    t.parentNode.replaceChild(cleanTable, t); // 'Format table' option
                }
                cleanElement(t);
                cleanTable.querySelectorAll('thead *, tbody *, table *').forEach((e) => {
                    cleanElement(e);
                });
 
                const trimChecked = document.getElementById('trim').checked;
                if (trimChecked) {
                    // trim ths in body
                    let tableDivTHs = cleanTable.querySelectorAll('th');
                    for (var th of tableDivTHs) { 
                        if (th.innerText) {
                            if (th.tagName.toLowerCase() === 'th') {
                                th.innerHTML = th.innerHTML.replaceAll(/\s{2,}/g, ' ').trim();
                                th.innerHTML = trimNBSP(th.innerHTML);
                                // th.innerHTML = th.innerHTML.replaceAll('&nbsp;', '').trim();
                            }
                        }                
                    }

                    // trim tds in body
                    let tableDivTDs = cleanTable.querySelectorAll('td');
                    for (var td of tableDivTDs) {
                        if (td.innerText) {
                            if (td.tagName.toLowerCase() === 'td') {
                                td.innerHTML = td.innerHTML.replaceAll(/\s{2,}/g, ' ').trim();
                                td.innerHTML = trimNBSP(td.innerHTML);
                            }
                        }    
                    }
                }

                const frNumbersChecked = document.getElementById('frNumbers').checked;
                if (frNumbersChecked) {
                    let tableDivTDs = cleanTable.querySelectorAll('td');
                    for (var td of tableDivTDs) {
                        if (td.innerText) {
                            if (td.tagName.toLowerCase() === 'td') {
                                const spaces = /(\d) (\d\d\d)/g;
                                const decimal = /(\d)\.(\d)/g;
                                while (td.innerHTML.match(/\d \d\d\d/)) td.innerHTML = td.innerHTML.replaceAll(spaces, "$1&nbsp;$2");
                                td.innerHTML = td.innerHTML.replaceAll(decimal, "$1,$2");
                            }
                        }    
                    }    
                }
            });

            tables = inputHTML.querySelectorAll('div.table-responsive');

            if (tables.length > 1) {
                createPagination(tables);
            } else {
                const tablePages = document.getElementById('table-pages');
                tablePages.innerHTML = "";
            }

            currentVisualTable = tables[0];

            // remove colgroup tag
            const colgroup = inputHTML.querySelector('colgroup');
            if (colgroup) {
                colgroup.parentNode.removeChild(colgroup);
            }

            loadCaptionFields();
            displayOutput(inputHTML);
            scrollToLine(getLineNumber(currentVisualTable.id));
            displayVisual(tables[0]);
        }
    }

    /**
     * 
     * @param {} tables 
     */
    function createPagination(tables) {
        if (!tables) return;

        const tablePages = document.getElementById('table-pages');
        tablePages.innerHTML = `<li class="disabled" ><a href="javascript: void(0)" id="prev-table">Previous</a></li>`;

        for (var i = 0; i < tables.length; i++) {
            tables[i].id = `t${i+1}`;
            tablePages.innerHTML += `<li${i==0 ? ' class="active"': ''} id="table-${i+1}"><a class="table-pagination">${i+1}</a></li>`;
        }

        tablePages.innerHTML += `<li class="disabled"><a href="javascript: void(0)" id="next-table">Next</a></li>`;

        createPaginationListener(tables);
    }


    function createPaginationListener(tables) {
        const tablePages = document.getElementById('table-pages');
        const pages = tablePages.querySelectorAll('li[id^="table-"]');
        
        for (var p of pages) {
            const a = p.querySelector('a.table-pagination');
            a.addEventListener('click', () => {
                var tableIndex = a.innerText;
                currentVisualTable = tables[tableIndex-1];

                loadCaptionFields();

                displayVisual(currentVisualTable);

                // Remove active on pagination and add active
                p.parentNode.querySelector('.active').classList.remove('active');
                a.parentNode.classList.add('active');

                // Add selection listeners to all table cells
                let cells = visual.querySelectorAll('th, td:not(tfoot td)');
                cells.forEach(e => {
                    addVisualCellListener(e);
                });

                console.log(`display table ${tableIndex}`);
                scrollToLine(getLineNumber(currentVisualTable.id));
                copyVisualHTML(currentVisualTable);
            });
        }
    }

    function loadCaptionFields() {
        const caption = currentVisualTable.querySelector('table > caption');

        const tableNum = document.getElementById('tableNum');
        const tableTitle = document.getElementById('tableTitle');
        const tableUnit = document.getElementById('tableUnit');

        if (caption) {
            // table num
            var visualTableNum = (caption.childNodes[0] && caption.childNodes[0].nodeType === Node.TEXT_NODE) ? caption.childNodes[0].nodeValue : "";
            tableNum.value = visualTableNum.trim();
            
            // table title
            const strong = caption.querySelector('strong');
            var visualTableTitle = (strong) ? strong.innerText : "";

            tableTitle.value = visualTableTitle.trim();
            
            // table unit
            const small = caption.querySelector('small');
            var visualTableUnit = (small) ? small.innerText : "";

            tableUnit.value = visualTableUnit.trim();
        } else {
            tableNum.value = "";
            tableTitle.value = "";
            tableUnit.value = "";
        }
    }

    function getLineNumber(id) {
        const output = document.getElementById('outputText');
        const lines = output.textContent.split('\n');
        const search = `id="${id}"`;
        const lineNumber = lines.findIndex(line => line.includes(search)) + 1;

        // console.log('Line number is', lineNumber);
        return lineNumber;
    }

    function scrollToLine(lineNumber) {
        const textarea = document.getElementById('outputText');
      
        // Create a temporary span to measure line height
        const temp = document.createElement('span');
        temp.style.visibility = 'hidden';
        temp.style.whiteSpace = 'pre';
        
        temp.style = textarea.style
        temp.textContent = 'A';
        document.body.appendChild(temp);
      
        const lineHeight = temp.getBoundingClientRect().height;
        document.body.removeChild(temp);
      
        // Scroll to the target line
        textarea.scrollTop = lineHeight * (lineNumber - 1);
      }
        
    /**
     * Removes all &nbsp; from the start and end of the string
     * 
     * @param {string} s input string
     * @returns string with all &nbsp; removed from the start
     */
     function trimNBSP(s) {
        s = s.trim();
        if (s == '&nbsp;') return s;
        while (s.startsWith('&nbsp;')) s = s.substring('&nbsp;'.length);
        while (s.endsWith('&nbsp;')) s = s.substring(0, s.length - '&nbsp;'.length);
        return s;
    }

    /**
     * Adds classes, elements, and attributes to format as WET Style table
     * @param {*} html HTML DOM element
     */
    function formatTable(inputTable) {
        const tableNumInput = document.getElementById('tableNum');

        const finTableChecked = document.getElementById('finTable').checked;
        const removeBoldChecked = document.getElementById('removeBold').checked;
        const scopeChecked = document.getElementById('scope').checked;
        const tfootChecked = document.getElementById('tfoot').checked;

        const table = inputTable.cloneNode(true);
        
        let newlineTab  = document.createTextNode('\n    ');

        // create 'table-responsive' div
        let tableDiv = document.createElement('div');
        tableDiv.classList.add('table-responsive');

        tableDiv.appendChild(table);
        tableDiv.insertBefore(newlineTab.cloneNode(), table);

        let tbody = table.querySelector('tbody');
        if (!tbody) return;

        if (!table.querySelector('thead')) {
            // Create 'thead' element before tbody
            let thead = document.createElement('thead');
            thead.appendChild(newlineTab);
    
            // get tbody and move 1st tr to thead
            table.insertBefore(thead, tbody);
            table.insertBefore(newlineTab.cloneNode(), tbody);
    
            tbody.querySelector('td').classList.toggle('selected');
            moveTRToThead(tbody.querySelectorAll('tr'), thead);
            thead.querySelector('th').classList.toggle('selected');
        } else {
            const tr = table.querySelector('thead > tr');
            tr.classList.add('bg-dark', 'text-white');
            tr.classList.remove('active');
    
            var count = 0;

            tr.querySelectorAll('th, td').forEach((node) => {
                if (finTableChecked && count > 0 && node.nodeName === 'TD') {
                    node.classList.add('text-right');
                }
                var newNode = renameTag(node, 'th');
                // add 'text-right' class to <th> if financial table
                if (scopeChecked) {
                    newNode.setAttribute('scope', 'col');
                }

                count++;
            });
        }
        
        // add 'table table-bordered' class to table
        table.classList.add('table', 'table-bordered');

        // add 'active' class to tr with colspans
        tbody.querySelectorAll('tr').forEach((tr) => {
            // add 'text-right' class to <th> if financial table
            if (finTableChecked) {
                tr.classList.add('text-right');
            }
            let trFirstChild = tr.querySelector('td, th');
            if (trFirstChild) {
                // rename first child to <th> tag
                var firstChildNode = renameTag(trFirstChild, 'th');
                if (scopeChecked) {
                    firstChildNode.setAttribute('scope', 'row');
                }

                // add 'active' class to <tr> if child has 'colspan'
                if (trFirstChild.hasAttribute('colspan')) {
                    tr.classList.add('active');
                    if (scopeChecked) {
                        trFirstChild.setAttribute('scope', 'colgroup');
                    }
                }
                if (scopeChecked && trFirstChild.hasAttribute('rowspan')) {
                    if (scopeChecked) {
                        trFirstChild.setAttribute('scope', 'rowgroup');
                    }
                }  
                else if (removeBoldChecked) {
                    firstChildNode.classList.add('fnt-nrml');
                }
            }
        });

        // tfoot option checked, create tfoot template
        if (tfootChecked) {
            addTfoot(table, true);
        }

        // // create unique ID, based on table num if exists
        // let uniqueID = 't00';
        // if (tableNumInput.value) {
        //     uniqueID = '';
        //     let parts = tableNumInput.value.split(' ');
        //     for (var s of parts) {
        //         uniqueID += `${s.charAt(0).replaceAll(/[^A-Za-z]/g, '').toLowerCase()}${s.replaceAll(/\D/g, '')}`; 
        //     }
        // }
        /*Disable Footnote functionality - Justin's fault*/
        //let tableCells = tableDiv.querySelectorAll('thead th, thead td, tbody th, tbody td');
        // for (var cell of tableCells) {
        //     generateFootnote(cell, table, uniqueID);
        // }

        return tableDiv;
    }

    /**
     * Adds a tfoot with preset DOM elements according to WET style
     * @param {*} table DOM HTML table
     */
    function addTfoot(table, placeholder) {
        let tfoot = document.createElement('tfoot');
        let tr = document.createElement('tr');
        let td = document.createElement('td');
        var p;
        if (placeholder)
            p = document.createElement('p');

        let newlineTab  = document.createTextNode('\n        ');
        let newlineTabSmall  = document.createTextNode('\n    ');
        let newline  = document.createTextNode('\n');

        // Calculate table width
        let tableWidth = 0;
        let rows = table.querySelectorAll('tr');
        for (var row of rows) {
            tableWidth = Math.max(row.querySelectorAll('th, td').length, tableWidth);
        }

        // set attributes
        tr.classList.add('small');
        td.setAttribute('colspan', tableWidth);

        if (placeholder)
            p.appendChild(document.createTextNode('NOTES, SOURCES and FOOTNOTES GO HERE'));

        // add all to table
        td.appendChild(newlineTab.cloneNode());
        if (placeholder)
            td.appendChild(p);
        td.appendChild(newlineTab.cloneNode());

        tr.appendChild(newlineTab);
        tr.appendChild(td);
        tr.appendChild(newlineTabSmall.cloneNode());

        tfoot.appendChild(newlineTabSmall);
        tfoot.appendChild(tr);
        tfoot.appendChild(newline.cloneNode());

        table.appendChild(newline);
        table.appendChild(tfoot);
        table.appendChild(newline.cloneNode());

        return tfoot;
    }

    /**
     * Replaces a node with a new node of a different tag
     * https://stackoverflow.com/a/15086834
     * @param {*} oldTag tag that will be replaced
     * @param {*} newTag name of new tag that will be created
     */
    function renameTag(oldTag, newTag) {
        let newNode = document.createElement(newTag);

        // Copy the children
        while (oldTag.firstChild) {
            newNode.appendChild(oldTag.firstChild); // *Moves* the child
        }

        // Copy the attributes
        if (oldTag.attributes) {
            for (index = oldTag.attributes.length - 1; index >= 0; --index) {
                newNode.attributes.setNamedItem(oldTag.attributes[index].cloneNode());
            }
        }

        // Replace it
        if (oldTag.parentNode) {
            oldTag.parentNode.replaceChild(newNode, oldTag);
        }

        newNode.addEventListener('click', () => {
            newNode.classList.toggle('selected');
            tableToolbar.classList.toggle('wb-inv', visual.querySelectorAll('.selected').length == 0);
        });

        return newNode;
    }

    /**
     * Removes attributes from DOM element or delete the element if in tag list
     * @param {*} e DOM element
     */
    function cleanElement(e) {


        if (e.hasAttributes()) {
            getAttributeList().forEach(attribute => e.removeAttribute(attribute));
        }

        getTagList().forEach(tag => removeTag(e, tag));
    }
    
    /**
     * Removes DOM if its tag name matches param
     * @param {*} e DOM element
     * @param {*} tag tag name to be removed
     */
    function removeTag(e, tag) {
        if (tag.toLowerCase() === 'p' && e.closest('tfoot')) {
            return;
        }

        if (e.tagName.toLowerCase() === tag.toLowerCase()) {
            const parent = e.parentNode;

            if (parent) {
                // // Copy the children
                // while (e.firstChild) {
                //     parent.appendChild(e.firstChild); // *Moves* the child
                // }
                
                // // Remove node
                // parent.removeChild(e);

                // Remove outer tags
                e.outerHTML = e.innerHTML;
            }
        }
    }

    function generateFootnote(e, t, uniqueID) {
        let newlineTab  = document.createTextNode('\n        ');
        let sup = e.querySelector('sup');

        // if element has a superscript
        if (sup) {
            // create footnote if not exist
            let tfoot = t.querySelector('tfoot');

            // create footnote elements
            if (!tfoot) {
                tfoot = addTfoot(t, true);
            }

            let aside = tfoot.querySelector('aside');
            let dl = tfoot.querySelector('dl');

            if (!aside) {
                aside = document.createElement('aside');
                dl = document.createElement('dl');
                
                // set attributes
                aside.classList.add('wb-fnote');
                aside.setAttribute('role','note');
                aside.appendChild(newlineTab.cloneNode());
                aside.appendChild(dl);
            }

            // create elements
            let supA = document.createElement('a');
            let dt = document.createElement('dt');
            let dd = document.createElement('dd');
            let p = document.createElement('p');
            let fnTextP = document.createElement('p');
            let fnText = document.createTextNode('Insert footnote text here');
            let a = document.createElement('a');
            let fnNum = sup.innerText;
            let fnNumText = document.createTextNode(fnNum);

            // create special sup tag
            sup.innerHTML = '';
            sup.id = `${uniqueID}-${fnNum}-rf`;
            supA.classList.add('fn-lnk');
            supA.href = `#${uniqueID}-${fnNum}`;

            supA.appendChild(fnNumText);
            sup.appendChild(supA);

            if (!aside.querySelector(`#${uniqueID}-${fnNum}`)){
                // tfoot footnotes
                dt.id = `${uniqueID}-${fnNum}-dt`;
                dd.id = `${uniqueID}-${fnNum}`;
                dd.setAttribute('tabindex', '-1');
                p.classList.add("fn-rtn");
                a.href = `#${uniqueID}-${fnNum}-rf`;

                a.appendChild(document.createTextNode(`${fnNum}`));
                p.appendChild(a);
                fnTextP.appendChild(fnText);
                dd.appendChild(fnTextP);
                dd.appendChild(p);
                dd.appendChild(newlineTab);

                let dts = dl.querySelectorAll('dt');
                for (var node of dts) {
                    var dtFnNum;
                    if (node && node.id) {
                        dtFnNum = parseInt(node.id.match(/[0-9]+/g)[1]);
                    }
                    if (node && node.id && fnNum < dtFnNum) {
                        dl.insertBefore(newlineTab.cloneNode(), node);
                        dl.insertBefore(dt, node);
                        dl.insertBefore(newlineTab.cloneNode(), node);
                        dl.insertBefore(dd, node);
                        break;
                    }
                    else {
                        dl.appendChild(newlineTab.cloneNode());
                        dl.appendChild(dt);
                        dl.appendChild(newlineTab.cloneNode());
                        dl.appendChild(dd);
                    }
                }

                if (dl.querySelectorAll('dt').length <= 0) {
                    dl.appendChild(newlineTab.cloneNode());
                    dl.appendChild(dt);
                    dl.appendChild(newlineTab.cloneNode());
                    dl.appendChild(dd);
                }
                tfoot.querySelector('tr').querySelector('td').appendChild(aside);
            }
        }   
    }

    /**
     * Gets an array of attributes from 'Clean Attributes' list, space-separated
     * @returns Array of attribute strings
     */
     function getAttributeList() {
        const attrText = document.getElementById('attrList');
        let attrs = attrText.value.trim().split('\n');
        return attrs;
    }
    
    /**
     * Gets an array of tags from 'Clean Tags' list, space-separated
     * @returns Array of tag strings
     */
    function getTagList() {
        const tagText = document.getElementById('tagList');
        tags = tagText.value.trim().split('\n');

        // check tags
        for (t of tags) {
            if (t.toLowerCase() === 'table') {
                const outputText = document.getElementById('outputText');

                let errorMsg = "Error: Not permitted to remove '" + t + "' tag";
                outputText.textContent = errorMsg;
                throw(errorMsg);
            }
        }
        return tags;
    }

    /**
     * Displays HTML code in Output Results
     * @param {*} html table html
     */
     function displayOutput(html) {
        const output = document.getElementById('outputText');
        output.textContent = html.innerHTML;
    }
    
    /**
     * Displays table HTML as the intended styled output in Visual Results
     * @param {*} html table html
     */
    function displayVisual(html) {
        const visual = document.getElementById('visual');
        visual.innerHTML = html.outerHTML;
    }

    /**
     * Clicking on the input element will copy its contents, present a message
     * and highlight the element.
     * @param {*} outputText DOM element containing the text to copy
     */
     function copyToClipboard(outputText) {
        const copyArea = document.getElementById('copyArea');
        const copiedLabel = document.getElementById('copiedLabel');

        outputText.style.boxShadow = "0px 0px 5px green";
        
        copyArea.textContent = outputText.innerText;

        copyArea.select();
        document.execCommand('copy');

        copiedLabel.classList.remove('wb-inv');
        
        // return to default label after 2000ms
        setTimeout(() => {
            outputText.style.boxShadow = null;
            copiedLabel.classList.add('wb-inv');
        }, 2000);
    }
    
    /**
     * Scrolls window to DOM element
     * @param {*} element DOM element to put into view
     */
    function scrollSmoothTo(element) {
        if (element) {
            element.scrollIntoView({
              block: 'start',
              behavior: 'smooth'
            });
        }
    }

    /*** DEFAULT VALUES START ***/
    const inputText = document.getElementById('inputText');
    // inputText.value = inputDefault.trim();
    inputText.value = getInputFromSession();

    const attrText = document.getElementById('attrList');
    attrText.value = attributesDefault.trim();

    const tagText = document.getElementById('tagList');
    tagText.value = tagsDefault.trim();
    /*** DEFAULT VALUES END ***/

    createListeners();
}
