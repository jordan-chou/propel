{
  "name": "propel",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/unit/**/*.test.js",
    "check": "node --check src/propel.js && node --check scripts/build-portable.mjs && node --check scripts/check-portable.mjs && node --check scripts/check-security.mjs && node scripts/check-security.mjs && node --test test/unit/**/*.test.js",
    "check:security": "node scripts/check-security.mjs",
    "build:portable": "node scripts/build-portable.mjs",
    "check:portable": "node scripts/check-portable.mjs",
    "verify:portable": "npm run build:portable && npm run check:portable"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
